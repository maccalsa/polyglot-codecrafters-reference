[
  {
    "id": "immutable-binding",
    "category": "Fundamentals",
    "concept": "Immutable binding",
    "equivalence": "partial",
    "snippets": {
      "Java": "final var x = 42;",
      "Kotlin": "val x = 42",
      "Python": "x = 42",
      "Go": "x := 42",
      "TypeScript": "const x = 42",
      "Rust": "let x = 42;",
      "Odin": "x := 42",
      "Gleam": "let x = 42"
    },
    "notes": {
      "Python": "No language-level const; convention and discipline only.",
      "Go": "The binding itself is mutable. There is no val/const for locals.",
      "Odin": ":= creates a variable. Use :: for compile-time constants."
    }
  },
  {
    "id": "constant",
    "category": "Fundamentals",
    "concept": "Constant",
    "equivalence": "same",
    "snippets": {
      "Java": "static final int PORT = 6379;",
      "Kotlin": "const val PORT = 6379",
      "Python": "PORT = 6379",
      "Go": "const Port = 6379",
      "TypeScript": "const PORT = 6379",
      "Rust": "const PORT: u16 = 6379;",
      "Odin": "PORT :: 6379",
      "Gleam": "const port = 6379"
    },
    "notes": {
      "Python": "Uppercase is convention only; the name remains assignable.",
      "Kotlin": "const val is compile-time. Use val for runtime constants."
    }
  },
  {
    "id": "mutable-variable",
    "category": "Fundamentals",
    "concept": "Mutable variable",
    "equivalence": "partial",
    "snippets": {
      "Java": "var x = 1;\nx = 2;",
      "Kotlin": "var x = 1\nx = 2",
      "Python": "x = 1\nx = 2",
      "Go": "x := 1\nx = 2",
      "TypeScript": "let x = 1\nx = 2",
      "Rust": "let mut x = 1;\nx = 2;",
      "Odin": "x := 1\nx = 2",
      "Gleam": "let x = 1\nlet x = 2"
    },
    "notes": {
      "Gleam": "Bindings are immutable. Reusing the name is shadowing, not mutation."
    }
  },
  {
    "id": "function",
    "category": "Fundamentals",
    "concept": "Function",
    "equivalence": "same",
    "snippets": {
      "Java": "int add(int a, int b) {\n  return a + b;\n}",
      "Kotlin": "fun add(a: Int, b: Int): Int = a + b",
      "Python": "def add(a: int, b: int) -> int:\n    return a + b",
      "Go": "func add(a, b int) int {\n    return a + b\n}",
      "TypeScript": "function add(a: number, b: number): number {\n  return a + b\n}",
      "Rust": "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}",
      "Odin": "add :: proc(a, b: int) -> int {\n    return a + b\n}",
      "Gleam": "fn add(a: Int, b: Int) -> Int {\n  a + b\n}"
    }
  },
  {
    "id": "lambda",
    "category": "Fundamentals",
    "concept": "Anonymous function / lambda",
    "equivalence": "same",
    "snippets": {
      "Java": "x -> x * 2",
      "Kotlin": "{ x -> x * 2 }",
      "Python": "lambda x: x * 2",
      "Go": "func(x int) int { return x * 2 }",
      "TypeScript": "(x: number) => x * 2",
      "Rust": "|x| x * 2",
      "Odin": "proc(x: int) -> int { return x * 2 }",
      "Gleam": "fn(x) { x * 2 }"
    }
  },
  {
    "id": "generic-function",
    "category": "Fundamentals",
    "concept": "Generic function",
    "equivalence": "same",
    "snippets": {
      "Java": "static <T> T id(T x) {\n  return x;\n}",
      "Kotlin": "fun <T> id(x: T): T = x",
      "Python": "def id[T](x: T) -> T:\n    return x",
      "Go": "func id[T any](x T) T {\n    return x\n}",
      "TypeScript": "function id<T>(x: T): T {\n  return x\n}",
      "Rust": "fn id<T>(x: T) -> T {\n    x\n}",
      "Odin": "id :: proc(x: $T) -> T {\n    return x\n}",
      "Gleam": "fn id(x: a) -> a {\n  x\n}"
    },
    "notes": {
      "Python": "Type-parameter syntax is 3.12+. Earlier: TypeVar."
    }
  },
  {
    "id": "tuple",
    "category": "Fundamentals",
    "concept": "Tuple / pair",
    "equivalence": "partial",
    "snippets": {
      "Java": "record Pair<A, B>(A first, B second) {}",
      "Kotlin": "val p = 1 to \"a\"",
      "Python": "p = (1, \"a\")",
      "Go": "return 1, \"a\"",
      "TypeScript": "const p: [number, string] = [1, \"a\"]",
      "Rust": "let p: (i32, &str) = (1, \"a\");",
      "Odin": "a, b := get_pair()",
      "Gleam": "let p = #(1, \"a\")"
    },
    "notes": {
      "Kotlin": "to builds Pair<A, B>.",
      "Go": "Multiple return values are the idiom; there is no first-class tuple.",
      "Odin": "No general tuple type. Use a small struct or multiple return values."
    }
  },
  {
    "id": "entry-point",
    "category": "Fundamentals",
    "concept": "Entry point / main",
    "equivalence": "same",
    "snippets": {
      "Java": "public static void main(String[] args) {\n  ...\n}",
      "Kotlin": "fun main(args: Array<String>) {\n  ...\n}",
      "Python": "def main() -> None:\n    ...\n\nif __name__ == \"__main__\":\n    main()",
      "Go": "func main() {\n    ...\n}",
      "TypeScript": "async function main() {\n  ...\n}\nmain()",
      "Rust": "fn main() {\n    ...\n}",
      "Odin": "main :: proc() {\n    ...\n}",
      "Gleam": "pub fn main() {\n  ...\n}"
    },
    "notes": {
      "Go": "Must live in package main.",
      "TypeScript": "Node treats the launched file as the entry. Browser/Deno differ."
    }
  },
  {
    "id": "record",
    "category": "Types",
    "concept": "Data carrier / record",
    "equivalence": "same",
    "snippets": {
      "Java": "record User(String name, int age) {}",
      "Kotlin": "data class User(val name: String, val age: Int)",
      "Python": "@dataclass\nclass User:\n    name: str\n    age: int",
      "Go": "type User struct {\n    Name string\n    Age  int\n}",
      "TypeScript": "type User = {\n  name: string\n  age: number\n}",
      "Rust": "struct User {\n    name: String,\n    age: u32,\n}",
      "Odin": "User :: struct {\n    name: string,\n    age: int,\n}",
      "Gleam": "pub type User {\n  User(name: String, age: Int)\n}"
    }
  },
  {
    "id": "interface",
    "category": "Types",
    "concept": "Interface / trait / protocol",
    "equivalence": "different",
    "snippets": {
      "Java": "interface Reader {\n  byte[] read();\n}",
      "Kotlin": "interface Reader {\n  fun read(): ByteArray\n}",
      "Python": "class Reader(Protocol):\n    def read(self) -> bytes: ...",
      "Go": "type Reader interface {\n    Read([]byte) (int, error)\n}",
      "TypeScript": "interface Reader {\n  read(): Uint8Array\n}",
      "Rust": "trait Reader {\n    fn read(&mut self) -> Vec<u8>;\n}",
      "Odin": "Reader :: struct {\n    read: proc(r: ^Reader) -> []u8,\n}",
      "Gleam": "pub fn read(r: Reader) -> BitArray {\n  r.read(r)\n}"
    },
    "notes": {
      "Go": "Satisfied implicitly; no implements clause.",
      "Odin": "No interface construct. Use procedure fields, tagged unions, or plain procedures.",
      "Gleam": "No typeclasses. Pass functions and custom types; do not look for implements."
    }
  },
  {
    "id": "enum",
    "category": "Types",
    "concept": "Enum / sum type",
    "equivalence": "partial",
    "snippets": {
      "Java": "sealed interface Msg permits Ping, Quit {}\nrecord Ping(int id) implements Msg {}\nrecord Quit() implements Msg {}",
      "Kotlin": "sealed interface Msg\ndata class Ping(val id: Int) : Msg\ndata object Quit : Msg",
      "Python": "type Msg = Ping | Quit",
      "Go": "type Msg interface{ isMsg() }\n\ntype Ping struct{ ID int }\ntype Quit struct{}\n\nfunc (Ping) isMsg() {}\nfunc (Quit) isMsg() {}",
      "TypeScript": "type Msg =\n  | { kind: \"ping\"; id: number }\n  | { kind: \"quit\" }",
      "Rust": "enum Msg {\n    Ping { id: i32 },\n    Quit,\n}",
      "Odin": "Msg :: union {\n    Ping,\n    Quit,\n}",
      "Gleam": "pub type Msg {\n  Ping(id: Int)\n  Quit\n}"
    },
    "notes": {
      "Python": "3.12+ type alias. Define Ping/Quit as dataclasses or NamedTuples.",
      "Go": "No native sum type. A sealed interface plus tagged structs is the usual stand-in."
    }
  },
  {
    "id": "optional",
    "category": "Types",
    "concept": "Optional / nullable",
    "equivalence": "partial",
    "snippets": {
      "Java": "Optional<String> name",
      "Kotlin": "val name: String?",
      "Python": "name: str | None",
      "Go": "var name *string",
      "TypeScript": "let name: string | undefined",
      "Rust": "let name: Option<String>",
      "Odin": "name: Maybe(string)",
      "Gleam": "let name: Option(String)"
    },
    "notes": {
      "Go": "Pointer or (value, ok). nil is not Optional.",
      "Odin": "Maybe(T) exists; many core APIs still use (value, ok)."
    }
  },
  {
    "id": "default-when-missing",
    "category": "Types",
    "concept": "Default when missing",
    "equivalence": "partial",
    "snippets": {
      "Java": "var n = opt.orElse(\"default\");",
      "Kotlin": "val n = value ?: \"default\"",
      "Python": "n = value if value is not None else \"default\"",
      "Go": "if value == \"\" {\n    value = \"default\"\n}",
      "TypeScript": "const n = value ?? \"default\"",
      "Rust": "let n = value.unwrap_or(\"default\");",
      "Odin": "n := value.? or_else \"default\"",
      "Gleam": "let n = option.unwrap(value, \"default\")"
    },
    "notes": {
      "Go": "Empty string is not absence. Prefer comma-ok on maps and pointers for missing values.",
      "Odin": "or_else is for Maybe(T). Comma-ok APIs need an explicit check."
    }
  },
  {
    "id": "if-else",
    "category": "Control flow",
    "concept": "If / else",
    "equivalence": "partial",
    "snippets": {
      "Java": "if (x > 0) {\n  ...\n} else {\n  ...\n}",
      "Kotlin": "if (x > 0) {\n  ...\n} else {\n  ...\n}",
      "Python": "if x > 0:\n    ...\nelse:\n    ...",
      "Go": "if x > 0 {\n    ...\n} else {\n    ...\n}",
      "TypeScript": "if (x > 0) {\n  ...\n} else {\n  ...\n}",
      "Rust": "if x > 0 {\n    ...\n} else {\n    ...\n}",
      "Odin": "if x > 0 {\n    ...\n} else {\n    ...\n}",
      "Gleam": "case x > 0 {\n  True -> ...\n  False -> ...\n}"
    },
    "notes": {
      "Gleam": "No if statement. case on Bool (or a richer type) is the branch."
    }
  },
  {
    "id": "pattern-match",
    "category": "Control flow",
    "concept": "Pattern match / switch",
    "equivalence": "partial",
    "snippets": {
      "Java": "switch (msg) {\n  case Ping p -> handle(p);\n  case Quit q -> stop();\n}",
      "Kotlin": "when (msg) {\n  is Ping -> handle(msg)\n  Quit -> stop()\n}",
      "Python": "match msg:\n    case Ping(id):\n        ...\n    case Quit():\n        ...",
      "Go": "switch v := msg.(type) {\ncase Ping:\n    _ = v\ncase Quit:\n    ...\n}",
      "TypeScript": "switch (msg.kind) {\n  case \"ping\":\n    ...\n    break\n  case \"quit\":\n    ...\n}",
      "Rust": "match msg {\n    Msg::Ping { id } => ...,\n    Msg::Quit => ...,\n}",
      "Odin": "switch v in msg {\ncase Ping:\n    ...\ncase Quit:\n    ...\n}",
      "Gleam": "case msg {\n  Ping(id) -> ...\n  Quit -> ...\n}"
    }
  },
  {
    "id": "for-each",
    "category": "Control flow",
    "concept": "For each",
    "equivalence": "same",
    "snippets": {
      "Java": "for (var x : xs) {\n  ...\n}",
      "Kotlin": "for (x in xs) {\n  ...\n}",
      "Python": "for x in xs:\n    ...",
      "Go": "for _, x := range xs {\n    ...\n}",
      "TypeScript": "for (const x of xs) {\n  ...\n}",
      "Rust": "for x in xs {\n    ...\n}",
      "Odin": "for x in xs {\n    ...\n}",
      "Gleam": "list.each(xs, fn(x) { ... })"
    },
    "notes": {
      "Gleam": "list.each is for side effects. Prefer list.map / list.filter for values."
    }
  },
  {
    "id": "index-iteration",
    "category": "Control flow",
    "concept": "Index + value iteration",
    "equivalence": "same",
    "snippets": {
      "Java": "for (int i = 0; i < xs.size(); i++) {\n  var x = xs.get(i);\n}",
      "Kotlin": "for ((i, x) in xs.withIndex()) {\n  ...\n}",
      "Python": "for i, x in enumerate(xs):\n    ...",
      "Go": "for i, x := range xs {\n    ...\n}",
      "TypeScript": "for (const [i, x] of xs.entries()) {\n  ...\n}",
      "Rust": "for (i, x) in xs.iter().enumerate() {\n    ...\n}",
      "Odin": "for x, i in xs {\n    ...\n}",
      "Gleam": "list.index_map(xs, fn(x, i) { ... })"
    },
    "notes": {
      "Gleam": "index_map returns a new list. It is not a side-effecting for-loop."
    }
  },
  {
    "id": "while",
    "category": "Control flow",
    "concept": "While loop",
    "equivalence": "partial",
    "snippets": {
      "Java": "while (condition) {\n  ...\n}",
      "Kotlin": "while (condition) {\n  ...\n}",
      "Python": "while condition:\n    ...",
      "Go": "for condition {\n    ...\n}",
      "TypeScript": "while (condition) {\n  ...\n}",
      "Rust": "while condition {\n    ...\n}",
      "Odin": "for condition {\n    ...\n}",
      "Gleam": "fn loop(n: Int) -> Int {\n  case n > 0 {\n    True -> loop(n - 1)\n    False -> n\n  }\n}"
    },
    "notes": {
      "Go": "for is the only loop. while condition is for condition { }.",
      "Odin": "Same as Go: for is while when given only a condition.",
      "Gleam": "No while. Recurse, or use list/iterator functions."
    }
  },
  {
    "id": "early-return",
    "category": "Control flow",
    "concept": "Early return",
    "equivalence": "different",
    "snippets": {
      "Java": "if (bad) {\n  return result;\n}",
      "Kotlin": "if (bad) return result",
      "Python": "if bad:\n    return result",
      "Go": "if bad {\n    return result\n}",
      "TypeScript": "if (bad) return result",
      "Rust": "if bad {\n    return result;\n}",
      "Odin": "if bad {\n    return result\n}",
      "Gleam": "use x <- result.try(parse(s))\nuse y <- result.try(validate(x))\nOk(y)"
    },
    "notes": {
      "Gleam": "No imperative early return. Structure with case or result.try pipelines."
    }
  },
  {
    "id": "list",
    "category": "Collections",
    "concept": "List / dynamic sequence",
    "equivalence": "partial",
    "snippets": {
      "Java": "var xs = new ArrayList<Integer>();",
      "Kotlin": "val xs = mutableListOf<Int>()",
      "Python": "xs: list[int] = []",
      "Go": "xs := []int{}",
      "TypeScript": "const xs: number[] = []",
      "Rust": "let mut xs: Vec<i32> = Vec::new();",
      "Odin": "xs := make([dynamic]int)\ndefer delete(xs)",
      "Gleam": "let xs: List(Int) = []"
    },
    "notes": {
      "Gleam": "Linked list, not a growable array. Prepend is cheap; append is not.",
      "Odin": "Dynamic arrays allocate. You own the memory until delete."
    }
  },
  {
    "id": "array",
    "category": "Collections",
    "concept": "Fixed / contiguous array",
    "equivalence": "partial",
    "snippets": {
      "Java": "int[] xs = new int[16];",
      "Kotlin": "val xs = IntArray(16)",
      "Python": "xs = [0] * 16",
      "Go": "var xs [16]int",
      "TypeScript": "const xs = new Int32Array(16)",
      "Rust": "let xs = [0i32; 16];",
      "Odin": "xs: [16]int",
      "Gleam": "let bytes: BitArray = <<0, 0, 0, 0>>"
    },
    "notes": {
      "Python": "list is the usual sequence. array.array / bytearray when you need packed storage.",
      "Gleam": "List is linked. BitArray is for bytes. No general mutable array in the core language."
    }
  },
  {
    "id": "map-create",
    "category": "Collections",
    "concept": "Map / dictionary creation",
    "equivalence": "same",
    "snippets": {
      "Java": "var m = new HashMap<String, Integer>();",
      "Kotlin": "val m = mutableMapOf<String, Int>()",
      "Python": "m: dict[str, int] = {}",
      "Go": "m := make(map[string]int)",
      "TypeScript": "const m = new Map<string, number>()",
      "Rust": "let mut m = HashMap::<String, i32>::new();",
      "Odin": "m := make(map[string]int)\ndefer delete(m)",
      "Gleam": "let m = dict.new()"
    },
    "notes": {
      "TypeScript": "Map is not a plain object. Objects do not have a reliable key order or typed keys."
    }
  },
  {
    "id": "map-lookup",
    "category": "Collections",
    "concept": "Map lookup with presence",
    "equivalence": "same",
    "snippets": {
      "Java": "var v = m.get(key);\nif (v != null) {\n  ...\n}",
      "Kotlin": "val v = m[key]\nif (v != null) {\n  ...\n}",
      "Python": "if key in m:\n    v = m[key]",
      "Go": "v, ok := m[key]\nif ok {\n    ...\n}",
      "TypeScript": "const v = m.get(key)\nif (v !== undefined) {\n  ...\n}",
      "Rust": "if let Some(v) = m.get(&key) {\n    ...\n}",
      "Odin": "v, ok := m[key]\nif ok {\n    ...\n}",
      "Gleam": "case dict.get(m, key) {\n  Ok(v) -> ...\n  Error(Nil) -> ...\n}"
    },
    "notes": {
      "Go": "m[key] without ok returns the zero value for missing keys."
    }
  },
  {
    "id": "set",
    "category": "Collections",
    "concept": "Set creation",
    "equivalence": "partial",
    "snippets": {
      "Java": "var s = new HashSet<String>();",
      "Kotlin": "val s = mutableSetOf<String>()",
      "Python": "s: set[str] = set()",
      "Go": "s := map[string]struct{}{}",
      "TypeScript": "const s = new Set<string>()",
      "Rust": "let mut s = HashSet::<String>::new();",
      "Odin": "s := make(map[string]struct{})\ndefer delete(s)",
      "Gleam": "let s = set.new()"
    },
    "notes": {
      "Go": "No set type. map[T]struct{} is the usual idiom.",
      "Odin": "Same map[T]struct{} idiom, or bit_set for enum-like keys."
    }
  },
  {
    "id": "filter",
    "category": "Collections",
    "concept": "Filter",
    "equivalence": "same",
    "snippets": {
      "Java": "var ys = xs.stream()\n    .filter(x -> x > 0)\n    .toList();",
      "Kotlin": "val ys = xs.filter { it > 0 }",
      "Python": "ys = [x for x in xs if x > 0]",
      "Go": "ys := make([]int, 0, len(xs))\nfor _, x := range xs {\n    if x > 0 {\n        ys = append(ys, x)\n    }\n}",
      "TypeScript": "const ys = xs.filter(x => x > 0)",
      "Rust": "let ys: Vec<_> = xs.into_iter()\n    .filter(|x| *x > 0)\n    .collect();",
      "Odin": "ys := make([dynamic]int)\nfor x in xs {\n    if x > 0 {\n        append(&ys, x)\n    }\n}",
      "Gleam": "let ys = list.filter(xs, fn(x) { x > 0 })"
    }
  },
  {
    "id": "map-transform",
    "category": "Collections",
    "concept": "Map / transform",
    "equivalence": "same",
    "snippets": {
      "Java": "var ys = xs.stream()\n    .map(x -> x * 2)\n    .toList();",
      "Kotlin": "val ys = xs.map { it * 2 }",
      "Python": "ys = [x * 2 for x in xs]",
      "Go": "ys := make([]int, len(xs))\nfor i, x := range xs {\n    ys[i] = x * 2\n}",
      "TypeScript": "const ys = xs.map(x => x * 2)",
      "Rust": "let ys: Vec<_> = xs.into_iter()\n    .map(|x| x * 2)\n    .collect();",
      "Odin": "ys := make([dynamic]int)\nfor x in xs {\n    append(&ys, x * 2)\n}",
      "Gleam": "let ys = list.map(xs, fn(x) { x * 2 })"
    }
  },
  {
    "id": "fold",
    "category": "Collections",
    "concept": "Fold / reduce",
    "equivalence": "same",
    "snippets": {
      "Java": "var total = xs.stream().reduce(0, Integer::sum);",
      "Kotlin": "val total = xs.fold(0) { acc, x -> acc + x }",
      "Python": "total = sum(xs)",
      "Go": "total := 0\nfor _, x := range xs {\n    total += x\n}",
      "TypeScript": "const total = xs.reduce((a, x) => a + x, 0)",
      "Rust": "let total: i32 = xs.iter().sum();",
      "Odin": "total := 0\nfor x in xs {\n    total += x\n}",
      "Gleam": "let total = list.fold(xs, 0, fn(acc, x) { acc + x })"
    }
  },
  {
    "id": "interpolation",
    "category": "Strings & bytes",
    "concept": "String interpolation",
    "equivalence": "same",
    "snippets": {
      "Java": "var s = \"port=\" + port;",
      "Kotlin": "val s = \"port=$port\"",
      "Python": "s = f\"port={port}\"",
      "Go": "s := fmt.Sprintf(\"port=%d\", port)",
      "TypeScript": "const s = `port=${port}`",
      "Rust": "let s = format!(\"port={port}\");",
      "Odin": "s := fmt.tprintf(\"port=%d\", port)",
      "Gleam": "let s = \"port=\" <> int.to_string(port)"
    },
    "notes": {
      "Java": "No interpolation in everyday Java. STR templates exist but are still preview/unstable across versions."
    }
  },
  {
    "id": "string-to-bytes",
    "category": "Strings & bytes",
    "concept": "UTF-8 string → bytes",
    "equivalence": "partial",
    "snippets": {
      "Java": "byte[] b = s.getBytes(StandardCharsets.UTF_8);",
      "Kotlin": "val b = s.encodeToByteArray()",
      "Python": "b = s.encode(\"utf-8\")",
      "Go": "b := []byte(s)",
      "TypeScript": "const b = new TextEncoder().encode(s)",
      "Rust": "let b = s.as_bytes();",
      "Odin": "b := transmute([]u8)s",
      "Gleam": "let b = bit_array.from_string(s)"
    },
    "notes": {
      "Rust": "as_bytes borrows. Clone with s.into_bytes() if you need an owned Vec<u8>.",
      "Odin": "transmute is a view. Do not mutate. Clone if you need ownership."
    }
  },
  {
    "id": "bytes-to-string",
    "category": "Strings & bytes",
    "concept": "Bytes → UTF-8 string",
    "equivalence": "partial",
    "snippets": {
      "Java": "var s = new String(b, StandardCharsets.UTF_8);",
      "Kotlin": "val s = b.decodeToString()",
      "Python": "s = b.decode(\"utf-8\")",
      "Go": "s := string(b)",
      "TypeScript": "const s = new TextDecoder().decode(b)",
      "Rust": "let s = std::str::from_utf8(&b)?;",
      "Odin": "s := string(b)",
      "Gleam": "let result = bit_array.to_string(b)"
    },
    "notes": {
      "Rust": "Fails on invalid UTF-8. Use from_utf8_lossy if you want replacement.",
      "Odin": "Conversion semantics and lifetime matter; clone if the bytes will move.",
      "Gleam": "Result(String, Nil)."
    }
  },
  {
    "id": "split",
    "category": "Strings & bytes",
    "concept": "Split string",
    "equivalence": "same",
    "snippets": {
      "Java": "var parts = s.split(\":\", -1);",
      "Kotlin": "val parts = s.split(\":\")",
      "Python": "parts = s.split(\":\")",
      "Go": "parts := strings.Split(s, \":\")",
      "TypeScript": "const parts = s.split(\":\")",
      "Rust": "let parts: Vec<_> = s.split(':').collect();",
      "Odin": "parts := strings.split(s, \":\")",
      "Gleam": "let parts = string.split(s, \":\")"
    },
    "notes": {
      "Java": "Limit -1 keeps trailing empty strings. The no-limit overload discards them."
    }
  },
  {
    "id": "parse-int",
    "category": "Strings & bytes",
    "concept": "Parse integer",
    "equivalence": "same",
    "snippets": {
      "Java": "int n = Integer.parseInt(s);",
      "Kotlin": "val n = s.toInt()",
      "Python": "n = int(s)",
      "Go": "n, err := strconv.Atoi(s)",
      "TypeScript": "const n = Number.parseInt(s, 10)",
      "Rust": "let n: i32 = s.parse()?;",
      "Odin": "n, ok := strconv.parse_int(s)",
      "Gleam": "let result = int.parse(s)"
    },
    "notes": {
      "Kotlin": "toInt() throws. toIntOrNull() if absence is expected.",
      "TypeScript": "Always pass radix 10. parseInt(\"08\") without it is not your friend in older JS.",
      "Odin": "Inspect the returned type/options for your compiler version.",
      "Gleam": "Result(Int, Nil)."
    }
  },
  {
    "id": "starts-with",
    "category": "Strings & bytes",
    "concept": "Starts-with / prefix",
    "equivalence": "same",
    "snippets": {
      "Java": "s.startsWith(\"+\")",
      "Kotlin": "s.startsWith(\"+\")",
      "Python": "s.startswith(\"+\")",
      "Go": "strings.HasPrefix(s, \"+\")",
      "TypeScript": "s.startsWith(\"+\")",
      "Rust": "s.starts_with('+')",
      "Odin": "strings.has_prefix(s, \"+\")",
      "Gleam": "string.starts_with(s, \"+\")"
    }
  },
  {
    "id": "trim",
    "category": "Strings & bytes",
    "concept": "Trim whitespace",
    "equivalence": "same",
    "snippets": {
      "Java": "s.trim()",
      "Kotlin": "s.trim()",
      "Python": "s.strip()",
      "Go": "strings.TrimSpace(s)",
      "TypeScript": "s.trim()",
      "Rust": "s.trim()",
      "Odin": "strings.trim_space(s)",
      "Gleam": "string.trim(s)"
    }
  },
  {
    "id": "slice",
    "category": "Strings & bytes",
    "concept": "Byte / string slice",
    "equivalence": "different",
    "snippets": {
      "Java": "var part = s.substring(i, j);\nvar bytes = Arrays.copyOfRange(b, i, j);",
      "Kotlin": "val part = s.substring(i, j)\nval bytes = b.copyOfRange(i, j)",
      "Python": "part = s[i:j]\nbytes = b[i:j]",
      "Go": "part := s[i:j]\nbytes := b[i:j]",
      "TypeScript": "const part = s.slice(i, j)\nconst bytes = b.subarray(i, j)",
      "Rust": "let part = &s[i..j];\nlet bytes = &b[i..j];",
      "Odin": "part := s[i:j]\nbytes := b[i:j]",
      "Gleam": "let part = string.slice(s, i, j - i)\nlet bytes = bit_array.slice(b, i, j - i)"
    },
    "notes": {
      "Go": "Slices share the backing array. A downstream append can mutate callers.",
      "Rust": "This is a borrow. The owner of s/b must outlive the slice.",
      "TypeScript": "subarray is a view; slice() on Uint8Array copies.",
      "Odin": "Slices are views. Copy if you need an independent buffer.",
      "Gleam": "slice takes start + length, not start + end."
    }
  },
  {
    "id": "append-bytes",
    "category": "Strings & bytes",
    "concept": "Append bytes to a buffer",
    "equivalence": "partial",
    "snippets": {
      "Java": "var buf = new ByteArrayOutputStream();\nbuf.write(data);",
      "Kotlin": "val buf = ByteArrayOutputStream()\nbuf.write(data)",
      "Python": "buf = bytearray()\nbuf.extend(data)",
      "Go": "buf = append(buf, data...)",
      "TypeScript": "buf = Buffer.concat([buf, data])",
      "Rust": "buf.extend_from_slice(data);",
      "Odin": "append(&buf, ..data)",
      "Gleam": "let buf = bit_array.append(buf, data)"
    },
    "notes": {
      "Go": "append may reallocate. Reassign the result; the old slice header can be stale.",
      "Gleam": "Immutable. append returns a new BitArray.",
      "TypeScript": "Node Buffer. In the browser, concatenate Uint8Array manually."
    }
  },
  {
    "id": "represent-error",
    "category": "Errors & resources",
    "concept": "Represent recoverable error",
    "equivalence": "different",
    "snippets": {
      "Java": "T method() throws IOException",
      "Kotlin": "fun method(): T",
      "Python": "def method() -> T:",
      "Go": "func method() (T, error)",
      "TypeScript": "function method(): T",
      "Rust": "fn method() -> Result<T, E>",
      "Odin": "method :: proc() -> (T, Error)",
      "Gleam": "fn method() -> Result(T, E)"
    },
    "notes": {
      "Kotlin": "Exceptions are unchecked. There is no throws clause.",
      "Python": "Raise an exception. Typing does not encode failure.",
      "TypeScript": "throw, or make failure explicit with a Result union.",
      "Odin": "Explicit multi-return is the common shape."
    }
  },
  {
    "id": "propagate-error",
    "category": "Errors & resources",
    "concept": "Propagate error",
    "equivalence": "different",
    "snippets": {
      "Java": "return method();",
      "Kotlin": "return method()",
      "Python": "return method()",
      "Go": "v, err := method()\nif err != nil {\n    return zero, err\n}",
      "TypeScript": "return method()",
      "Rust": "let v = method()?;",
      "Odin": "v, err := method()\nif err != nil {\n    return {}, err\n}",
      "Gleam": "use v <- result.try(method())"
    },
    "notes": {
      "Java": "Or declare throws and let it escape.",
      "Kotlin": "Unchecked exceptions propagate automatically.",
      "Python": "Exceptions propagate automatically.",
      "TypeScript": "Thrown exceptions propagate. Result unions do not.",
      "Gleam": "Or case method() { Ok(v) -> ... Error(e) -> Error(e) }."
    }
  },
  {
    "id": "cleanup",
    "category": "Errors & resources",
    "concept": "Cleanup / defer",
    "equivalence": "partial",
    "snippets": {
      "Java": "try (var in = open()) {\n  ...\n}",
      "Kotlin": "open().use { resource ->\n  ...\n}",
      "Python": "with open_resource() as resource:\n    ...",
      "Go": "r := open()\ndefer r.Close()",
      "TypeScript": "const r = open()\ntry {\n  ...\n} finally {\n  r.close()\n}",
      "Rust": "let r = open()?;",
      "Odin": "r := open()\ndefer close(r)",
      "Gleam": "use _ <- result.try(open())"
    },
    "notes": {
      "Rust": "Drop runs at end of scope. No finally needed for owned resources.",
      "TypeScript": "No language-level defer. try/finally, or runtime helpers (using, Disposable).",
      "Gleam": "Managed runtime. Resource APIs are library-specific; there is no defer."
    }
  },
  {
    "id": "assertion",
    "category": "Errors & resources",
    "concept": "Assertion",
    "equivalence": "partial",
    "snippets": {
      "Java": "assert x > 0;",
      "Kotlin": "check(x > 0)",
      "Python": "assert x > 0",
      "Go": "if x <= 0 {\n    panic(\"assert\")\n}",
      "TypeScript": "if (!(x > 0)) {\n  throw new Error(\"assertion failed\")\n}",
      "Rust": "assert!(x > 0);",
      "Odin": "assert(x > 0)",
      "Gleam": "let assert True = x > 0"
    },
    "notes": {
      "Java": "JVM assertions are off unless -ea.",
      "Kotlin": "check throws. assert may depend on JVM -ea.",
      "Go": "No built-in assert. Use an explicit if / panic, or a test helper."
    }
  },
  {
    "id": "cli-args",
    "category": "I/O & process",
    "concept": "CLI arguments",
    "equivalence": "partial",
    "snippets": {
      "Java": "public static void main(String[] args) {\n  ...\n}",
      "Kotlin": "fun main(args: Array<String>) {\n  ...\n}",
      "Python": "args = sys.argv[1:]",
      "Go": "args := os.Args[1:]",
      "TypeScript": "const args = process.argv.slice(2)",
      "Rust": "let args: Vec<String> = std::env::args().skip(1).collect();",
      "Odin": "args := os.args",
      "Gleam": "let args = argv.load().arguments"
    },
    "notes": {
      "TypeScript": "Node. Deno uses Deno.args.",
      "Gleam": "argv package, or gleam_erlang/os. Not in gleam_stdlib."
    }
  },
  {
    "id": "env-var",
    "category": "I/O & process",
    "concept": "Environment variable",
    "equivalence": "partial",
    "snippets": {
      "Java": "var v = System.getenv(\"PORT\");",
      "Kotlin": "val v = System.getenv(\"PORT\")",
      "Python": "v = os.getenv(\"PORT\")",
      "Go": "v, ok := os.LookupEnv(\"PORT\")",
      "TypeScript": "const v = process.env.PORT",
      "Rust": "let v = std::env::var(\"PORT\");",
      "Odin": "v, found := os.lookup_env(\"PORT\", context.allocator)\ndefer delete(v)",
      "Gleam": "let result = envoy.get(\"PORT\")"
    },
    "notes": {
      "TypeScript": "Node. Deno uses Deno.env.get.",
      "Rust": "Result. Err if unset.",
      "Gleam": "envoy or gleam_erlang. Target-specific; not in gleam_stdlib."
    }
  },
  {
    "id": "read-text-file",
    "category": "I/O & process",
    "concept": "Read whole text file",
    "equivalence": "partial",
    "snippets": {
      "Java": "var s = Files.readString(path);",
      "Kotlin": "val s = File(path).readText()",
      "Python": "s = Path(path).read_text()",
      "Go": "b, err := os.ReadFile(path)\ns := string(b)",
      "TypeScript": "const s = await readFile(path, \"utf8\")",
      "Rust": "let s = std::fs::read_to_string(path)?;",
      "Odin": "data, err := os.read_entire_file(path, context.allocator)\ndefer delete(data)",
      "Gleam": "let result = simplifile.read(path)"
    },
    "notes": {
      "TypeScript": "node:fs/promises. Browser has no filesystem.",
      "Gleam": "simplifile is the usual package. Erlang vs JS backends differ."
    }
  },
  {
    "id": "stdin-stdout",
    "category": "I/O & process",
    "concept": "Stdin / stdout",
    "equivalence": "same",
    "snippets": {
      "Java": "var line = new BufferedReader(\n    new InputStreamReader(System.in)).readLine();\nSystem.out.print(s);",
      "Kotlin": "val line = readln()\nprint(s)",
      "Python": "line = sys.stdin.readline()\nsys.stdout.write(s)",
      "Go": "line, err := bufio.NewReader(os.Stdin).ReadString('\\n')\nfmt.Print(s)",
      "TypeScript": "const line = await once(process.stdin, \"data\")\nprocess.stdout.write(s)",
      "Rust": "io::stdin().read_line(&mut line)?;\nprint!(\"{s}\");",
      "Odin": "fmt.print(s)\n// read from os.stdin with a buffered helper",
      "Gleam": "io.println(s)"
    },
    "notes": {
      "TypeScript": "Node streams. once() is node:events.",
      "Gleam": "gleam/io covers stdout. Stdin is target/package-specific."
    }
  },
  {
    "id": "read-binary-file",
    "category": "I/O & process",
    "concept": "Read binary file",
    "equivalence": "partial",
    "snippets": {
      "Java": "byte[] b = Files.readAllBytes(path);",
      "Kotlin": "val b = File(path).readBytes()",
      "Python": "b = Path(path).read_bytes()",
      "Go": "b, err := os.ReadFile(path)",
      "TypeScript": "const b = await readFile(path)",
      "Rust": "let b = std::fs::read(path)?;",
      "Odin": "data, err := os.read_entire_file(path, context.allocator)\ndefer delete(data)",
      "Gleam": "let result = simplifile.read_bits(path)"
    },
    "notes": {
      "TypeScript": "node:fs/promises returns Buffer.",
      "Gleam": "simplifile.read_bits. Confirm the name for your package version."
    }
  },
  {
    "id": "write-file",
    "category": "I/O & process",
    "concept": "Write file",
    "equivalence": "partial",
    "snippets": {
      "Java": "Files.write(path, bytes);",
      "Kotlin": "File(path).writeBytes(bytes)",
      "Python": "Path(path).write_bytes(data)",
      "Go": "err := os.WriteFile(path, data, 0644)",
      "TypeScript": "await writeFile(path, data)",
      "Rust": "std::fs::write(path, data)?;",
      "Odin": "ok := os.write_entire_file(path, data)",
      "Gleam": "let result = simplifile.write_bits(path, data)"
    },
    "notes": {
      "TypeScript": "node:fs/promises.",
      "Gleam": "simplifile. Erlang vs JS backends differ.",
      "Odin": "Confirm the exact write helper for your compiler version."
    }
  },
  {
    "id": "test",
    "category": "Testing",
    "concept": "Test",
    "equivalence": "same",
    "snippets": {
      "Java": "@Test\nvoid parses() {\n  assertEquals(1, parse(\"1\"));\n}",
      "Kotlin": "@Test\nfun parses() {\n  assertEquals(1, parse(\"1\"))\n}",
      "Python": "def test_parses():\n    assert parse(\"1\") == 1",
      "Go": "func TestParse(t *testing.T) {\n    if got := parse(\"1\"); got != 1 {\n        t.Fatal(got)\n    }\n}",
      "TypeScript": "test(\"parses\", () => {\n  expect(parse(\"1\")).toBe(1)\n})",
      "Rust": "#[test]\nfn parses() {\n    assert_eq!(1, parse(\"1\"));\n}",
      "Odin": "@(test)\ntest_parses :: proc(t: ^testing.T) {\n    testing.expect_value(t, parse(\"1\"), 1)\n}",
      "Gleam": "pub fn parses_test() {\n  assert parse(\"1\") == 1\n}"
    },
    "notes": {
      "TypeScript": "Runner-dependent (node:test, vitest, jest).",
      "Gleam": "File lives under test/ and ends in _test.gleam. Generated projects call gleeunit.main()."
    }
  },
  {
    "id": "run-tests",
    "category": "Testing",
    "concept": "Run tests",
    "equivalence": "same",
    "snippets": {
      "Java": "./gradlew test",
      "Kotlin": "./gradlew test",
      "Python": "pytest\npython -m unittest",
      "Go": "go test ./...",
      "TypeScript": "npm test",
      "Rust": "cargo test",
      "Odin": "odin test .",
      "Gleam": "gleam test"
    },
    "notes": {
      "Python": "pytest is common; unittest is stdlib.",
      "TypeScript": "Script and runner are project-dependent."
    }
  },
  {
    "id": "tcp-listen",
    "category": "Networking",
    "concept": "TCP listen",
    "equivalence": "partial",
    "snippets": {
      "Java": "var server = new ServerSocket(port);",
      "Kotlin": "val server = ServerSocket(port)",
      "Python": "s = socket.socket()\ns.bind((\"127.0.0.1\", port))\ns.listen()",
      "Go": "ln, err := net.Listen(\"tcp\", fmt.Sprintf(\":%d\", port))",
      "TypeScript": "const server = net.createServer(handler)\nserver.listen(port)",
      "Rust": "let listener = TcpListener::bind((\"127.0.0.1\", port))?;",
      "Odin": "server, err := net.listen_tcp({net.IP4_Any, port})",
      "Gleam": "glisten.new(init, loop)\n|> glisten.start(port)"
    },
    "notes": {
      "TypeScript": "Node net. createServer already takes the connection handler.",
      "Gleam": "No TCP in gleam_stdlib. On BEAM, glisten (or the CodeCrafters starter). Confirm the builder API for your version."
    }
  },
  {
    "id": "tcp-accept",
    "category": "Networking",
    "concept": "TCP accept / connection loop",
    "equivalence": "different",
    "snippets": {
      "Java": "while (true) {\n  var socket = server.accept();\n  ...\n}",
      "Kotlin": "while (true) {\n  val socket = server.accept()\n  ...\n}",
      "Python": "while True:\n    conn, addr = s.accept()\n    ...",
      "Go": "for {\n    conn, err := ln.Accept()\n    ...\n}",
      "TypeScript": "net.createServer(socket => {\n  ...\n})",
      "Rust": "for stream in listener.incoming() {\n    let stream = stream?;\n    ...\n}",
      "Odin": "for {\n    client, source, err := net.accept_tcp(server)\n    ...\n}",
      "Gleam": "fn loop(state, msg, conn) {\n  // glisten calls this per connection event\n}"
    },
    "notes": {
      "TypeScript": "Node registers a callback. There is no blocking accept loop.",
      "Gleam": "glisten owns acceptors. You write a handler, not an accept loop."
    }
  },
  {
    "id": "tcp-read",
    "category": "Networking",
    "concept": "Read bytes from TCP connection",
    "equivalence": "partial",
    "snippets": {
      "Java": "int n = in.read(buffer);",
      "Kotlin": "val n = input.read(buffer)",
      "Python": "data = conn.recv(4096)",
      "Go": "n, err := conn.Read(buf)",
      "TypeScript": "socket.on(\"data\", (chunk: Buffer) => {\n  ...\n})",
      "Rust": "let n = stream.read(&mut buf)?;",
      "Odin": "n, err := net.recv_tcp(client, buf)",
      "Gleam": "Packet(data) -> {\n  // BitArray from the connection\n}"
    },
    "notes": {
      "Java": "n == -1 is EOF. n may be smaller than buffer.length.",
      "TypeScript": "Push, not pull. You accumulate chunks; you do not call read().",
      "Gleam": "Package-specific messages, typically BitArray."
    }
  },
  {
    "id": "tcp-write",
    "category": "Networking",
    "concept": "Write bytes to TCP connection",
    "equivalence": "partial",
    "snippets": {
      "Java": "out.write(bytes);",
      "Kotlin": "output.write(bytes)",
      "Python": "conn.sendall(data)",
      "Go": "_, err := conn.Write(data)",
      "TypeScript": "socket.write(data)",
      "Rust": "stream.write_all(data)?;",
      "Odin": "n, err := net.send_tcp(client, data)",
      "Gleam": "glisten.send(conn, data)"
    },
    "notes": {
      "Go": "Write can be short. See write-all.",
      "Python": "sendall retries until all bytes are queued.",
      "TypeScript": "Node. write() may buffer; listen for drain if you flood the socket.",
      "Gleam": "Name and arguments are package-version specific."
    }
  },
  {
    "id": "tcp-connect",
    "category": "Networking",
    "concept": "TCP client connect",
    "equivalence": "partial",
    "snippets": {
      "Java": "var socket = new Socket(\"127.0.0.1\", port);",
      "Kotlin": "val socket = Socket(\"127.0.0.1\", port)",
      "Python": "conn = socket.create_connection((\"127.0.0.1\", port))",
      "Go": "conn, err := net.Dial(\"tcp\", addr)",
      "TypeScript": "const socket = net.createConnection({ host, port })",
      "Rust": "let stream = TcpStream::connect((\"127.0.0.1\", port))?;",
      "Odin": "client, err := net.dial_tcp({net.IP4_Loopback, port})",
      "Gleam": "// gleam add mug\nmug.connect(host, port, timeout_ms)"
    },
    "notes": {
      "TypeScript": "Node net.",
      "Gleam": "No stdlib client. mug is a common BEAM TCP client; confirm the API.",
      "Odin": "Confirm dial helper and address type for your compiler version."
    }
  },
  {
    "id": "tcp-close",
    "category": "Networking",
    "concept": "Close / shutdown connection",
    "equivalence": "same",
    "snippets": {
      "Java": "socket.close();",
      "Kotlin": "socket.close()",
      "Python": "conn.close()",
      "Go": "conn.Close()",
      "TypeScript": "socket.end()",
      "Rust": "stream.shutdown(Shutdown::Both)?;",
      "Odin": "net.close(client)",
      "Gleam": "glisten.close(conn)"
    },
    "notes": {
      "TypeScript": "end() half-closes after flushing. destroy() is abrupt.",
      "Rust": "Drop also closes. shutdown is explicit half/full close.",
      "Gleam": "Package-specific. Confirm the close helper."
    }
  },
  {
    "id": "buffered-read",
    "category": "Networking",
    "concept": "Read until delimiter or N bytes",
    "equivalence": "partial",
    "snippets": {
      "Java": "var reader = new BufferedReader(\n    new InputStreamReader(in, StandardCharsets.UTF_8));\nString line = reader.readLine();",
      "Kotlin": "val line = input.bufferedReader().readLine()",
      "Python": "line = conn.makefile().readline()\nchunk = conn.recv(n)",
      "Go": "r := bufio.NewReader(conn)\nline, err := r.ReadBytes('\\n')\nchunk := make([]byte, n)\n_, err = io.ReadFull(r, chunk)",
      "TypeScript": "buf = Buffer.concat([buf, chunk])\nconst i = buf.indexOf(\"\\n\")\nif (i >= 0) {\n  const line = buf.subarray(0, i)\n  buf = buf.subarray(i + 1)\n}",
      "Rust": "let mut r = BufReader::new(&stream);\nr.read_until(b'\\n', &mut line)?;\nr.read_exact(&mut chunk)?;",
      "Odin": "// accumulate in a buffer; scan for \\n\nn, err := net.recv_tcp(client, buf[filled:])",
      "Gleam": "let combined = bit_array.append(buf, chunk)\n// split on <<\"\\n\">> yourself"
    },
    "notes": {
      "Java": "readLine() strips the newline and returns null at EOF.",
      "TypeScript": "You must reassemble frames. data events are not message boundaries.",
      "Odin": "No std bufio. Keep a filled count and compact the buffer after each frame.",
      "Gleam": "If the package already delivers messages, still buffer if a frame can split."
    }
  },
  {
    "id": "write-all",
    "category": "Networking",
    "concept": "Write-all / short writes",
    "equivalence": "partial",
    "snippets": {
      "Java": "out.write(bytes);\nout.flush();",
      "Kotlin": "output.write(bytes)\noutput.flush()",
      "Python": "conn.sendall(data)",
      "Go": "_, err := io.Copy(conn, bytes.NewReader(data))",
      "TypeScript": "const ok = socket.write(data)\nif (!ok) socket.once(\"drain\", resume)",
      "Rust": "stream.write_all(data)?;",
      "Odin": "for remaining := data; len(remaining) > 0; {\n    n, err := net.send_tcp(client, remaining)\n    remaining = remaining[n:]\n}",
      "Gleam": "glisten.send(conn, data)"
    },
    "notes": {
      "Java": "OutputStream.write(byte[]) writes the whole array or throws. Still flush sockets.",
      "Go": "conn.Write can return a short write without err == nil. Loop or use io.Copy.",
      "TypeScript": "write() returning false means the kernel buffer is full, not that data was lost.",
      "Gleam": "Treat send as package-defined; do not assume POSIX short writes."
    }
  },
  {
    "id": "timeout",
    "category": "Networking",
    "concept": "Timeout / deadline",
    "equivalence": "partial",
    "snippets": {
      "Java": "socket.setSoTimeout(1000);",
      "Kotlin": "socket.soTimeout = 1000",
      "Python": "conn.settimeout(1.0)",
      "Go": "conn.SetDeadline(time.Now().Add(time.Second))",
      "TypeScript": "socket.setTimeout(1000)",
      "Rust": "stream.set_read_timeout(Some(Duration::from_secs(1)))?;",
      "Odin": "net.set_option(client, net.Receive_Timeout, 1 * time.Second)",
      "Gleam": "process.send_after(self(), 1000, Timeout)"
    },
    "notes": {
      "Java": "SoTimeout applies to blocking reads; it throws SocketTimeoutException.",
      "Go": "Deadline is absolute. Reset it on each request if you want a per-read timeout.",
      "TypeScript": "Fires a timeout event; it does not throw from write().",
      "Odin": "Option names are version-sensitive. Compiler-check this.",
      "Gleam": "BEAM process timers, not socket options. Package APIs may wrap this."
    }
  },
  {
    "id": "spawn",
    "category": "Concurrency",
    "concept": "Spawn concurrent work",
    "equivalence": "different",
    "snippets": {
      "Java": "Thread.startVirtualThread(() -> work());",
      "Kotlin": "launch { work() }",
      "Python": "threading.Thread(target=work).start()",
      "Go": "go work()",
      "TypeScript": "await work()",
      "Rust": "std::thread::spawn(|| work());",
      "Odin": "thread.create_and_start(proc() { work() })",
      "Gleam": "process.spawn(fn() { work() })"
    },
    "notes": {
      "Java": "Virtual threads are Java 21+. Older: new Thread(work).start().",
      "Kotlin": "Needs a coroutine scope. launch is not a language keyword you can call anywhere.",
      "Python": "Or asyncio.create_task(...) for cooperative async.",
      "TypeScript": "The event loop already interleaves I/O. Use Worker for CPU parallelism.",
      "Odin": "See core:thread; exact helpers vary by version.",
      "Gleam": "gleam_erlang on BEAM. JS target has no process.spawn."
    }
  },
  {
    "id": "channel",
    "category": "Concurrency",
    "concept": "Channel / message passing",
    "equivalence": "different",
    "snippets": {
      "Java": "var q = new LinkedBlockingQueue<T>();",
      "Kotlin": "val ch = Channel<T>()",
      "Python": "q = queue.Queue()",
      "Go": "ch := make(chan T)",
      "TypeScript": "const q: T[] = []",
      "Rust": "let (tx, rx) = std::sync::mpsc::channel();",
      "Odin": "c, _ := chan.create(chan.Chan(MyType), context.allocator)\ndefer chan.destroy(c)",
      "Gleam": "process.send(pid, msg)\nlet selector = process.new_selector()"
    },
    "notes": {
      "TypeScript": "No built-in CSP channel. Use an async queue or a library.",
      "Odin": "core:sync/chan. Confirm create/destroy for your version.",
      "Gleam": "BEAM processes plus typed selectors via gleam_erlang / OTP libraries."
    }
  }
]
